Character.isLowerCase(ch)

char[] ch = phone_number.toCharArray();
     for(int i = 0; i < ch.length - 4; i ++){
         ch[i] = '*';
     }
     return String.valueOf(ch);
char[]를 String으로 바꾸는 메서드이다.


phone_number.replaceAll(".(?=.{4})", "*");
정규식 솜씨가 대단하다
